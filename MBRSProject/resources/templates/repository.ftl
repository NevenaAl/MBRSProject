package com.example.beapp.user.repositories;

import org.springframework.stereotype.Repository;
import com.example.beapp.generated.repositories.${class.name}BaseRepository;

@Repository
public interface ${class.name}Repository extends ${class.name}BaseRepository {

}